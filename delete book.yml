info:
  name: delete book
  type: http
  seq: 10

http:
  method: DELETE
  url: http://localhost:9000/api/books/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
