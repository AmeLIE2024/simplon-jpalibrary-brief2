info:
  name: get book by title
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:9000/api/books/title/Le hobbit
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
